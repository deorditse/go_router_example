import 'package:flutter/material.dart';

class MyMaterialSkeletonPage extends StatelessWidget {
  const MyMaterialSkeletonPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
